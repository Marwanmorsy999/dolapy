(()=>{'use strict';
const STORE='dolapy.pages.v3',BG_URL='https://cdn.jsdelivr.net/npm/@imgly/background-removal@1.7.0/+esm',TF_URL='https://cdn.jsdelivr.net/npm/@huggingface/transformers@4.2.0',MODEL='ff13/fashion-clip',$=s=>document.querySelector(s);let bg,classifier,loading=null,cameraStream=null,current=null,queue=[];
const P=()=>window.DolapyPerformance||{imageSize:()=>768,segmentationModel:()=> 'isnet_quint8',classifierOptions:()=>navigator.gpu?{device:'webgpu',dtype:'fp16'}:{device:'wasm',dtype:'q8'},shouldRunSecondPass:s=>s<.35,compositeLimit:()=>4};
const LABELS=['t-shirt','graphic t-shirt','polo shirt','button-up shirt','shirt','hoodie','sweater','cardigan','jacket','coat','blazer','overshirt','jeans','wide-leg trousers','trousers','cargo pants','chinos','shorts','skirt','dress','suit','sneakers','boots','loafers','sandals','heels','slides','bag','backpack','cap','hat','belt','watch','scarf','glasses'];
const COLORS={black:['black','charcoal','graphite'],white:['white','cream','ivory'],grey:['grey','gray','silver'],neutral:['beige','tan','camel','khaki','sand','stone','oat'],brown:['brown','chocolate','mocha','coffee'],blue:['navy','blue','denim','cobalt','teal','sky','azure'],green:['green','olive','sage','forest','mint'],red:['red','burgundy','maroon','wine','crimson'],orange:['orange','rust','terracotta','coral'],yellow:['yellow','mustard','gold'],purple:['purple','lavender','lilac','violet'],pink:['pink','rose','blush']};
const title=s=>String(s||'').replace(/(^|[\s-])([a-z])/g,(m,p,c)=>p+c.toUpperCase()),uid=()=>globalThis.crypto?.randomUUID?.()||`${Date.now()}-${Math.random().toString(36).slice(2)}`,clamp=(n,a=0,b=1)=>Math.max(a,Math.min(b,Number(n)||0));
function status(show,label='',pct=0,sub=''){let el=$('#aiScanStatus');if(!el){el=document.createElement('div');el.id='aiScanStatus';el.className='scan-status';el.hidden=true;el.innerHTML='<div class="scan-card"><div class="scan-spinner"></div><div class="scan-copy"><strong id="aiScanLabel"></strong><span id="aiScanSub"></span></div><div class="scan-track"><span id="aiScanBar"></span></div></div>';document.body.appendChild(el)}el.hidden=!show;if(show){$('#aiScanLabel').textContent=label;$('#aiScanSub').textContent=sub;$('#aiScanBar').style.width=`${clamp(pct,0,100)}%`}}
async function loadAI(){if(loading)return loading;loading=(async()=>{const[b,t]=await Promise.all([import(BG_URL),import(TF_URL)]);bg=b.removeBackground;classifier=await t.pipeline('zero-shot-image-classification',MODEL,P().classifierOptions());return true})().catch(e=>{loading=null;throw e});return loading}
window.warmDolapyAI=()=>loadAI().catch(e=>console.warn('Dolapy AI warmup failed',e));
async function toBlob(file,max=P().imageSize()){return new Promise((resolve,reject)=>{const r=new FileReader();r.onerror=reject;r.onload=()=>{const im=new Image();im.onerror=reject;im.onload=()=>{const k=Math.min(1,max/Math.max(im.naturalWidth,im.naturalHeight)),c=document.createElement('canvas');c.width=Math.max(1,Math.round(im.naturalWidth*k));c.height=Math.max(1,Math.round(im.naturalHeight*k));c.getContext('2d').drawImage(im,0,0,c.width,c.height);c.toBlob(b=>b?resolve(b):reject(new Error('encode failed')),'image/jpeg',.78)};im.src=r.result};r.readAsDataURL(file)})}
const dataUrl=b=>new Promise((resolve,reject)=>{const r=new FileReader();r.onerror=reject;r.onload=()=>resolve(r.result);r.readAsDataURL(b)});
function hsl(r,g,b){const R=r/255,G=g/255,B=b/255,mx=Math.max(R,G,B),mn=Math.min(R,G,B),d=mx-mn,l=(mx+mn)/2,s=d===0?0:d/(1-Math.abs(2*l-1));let h=0;if(d){if(mx===R)h=((G-B)/d)%6;else if(mx===G)h=(B-R)/d+2;else h=(R-G)/d+4;h=(h*60+360)%360}return{h,s,l,v:mx}}
function colorName(r,g,b){const{h,s,l,v}=hsl(r,g,b);if(v<.16)return'black';if(v<.28&&s<.28)return'charcoal';if(l>.92&&s<.13)return'white';if(l>.82&&s<.24)return'cream';if(s<.1&&l<.7)return'grey';if(s<.2&&l>=.7)return'beige';if(l<.42&&h>=15&&h<45&&s>.22)return'brown';if(h>=345||h<12)return l<.45?'burgundy':'red';if(h<42)return l<.42?'rust':'orange';if(h<72)return l<.45?'mustard':'yellow';if(h<160)return l<.45?'olive':'green';if(h<255)return l<.42?'navy':'blue';if(h<310)return l<.46?'purple':'lavender';return l<.5?'rose':'pink'}
const family=name=>{const s=String(name||'').toLowerCase();for(const[k,ws]of Object.entries(COLORS))if(ws.some(w=>s.includes(w)))return k;return'unknown'};
function cleanCutout(blob){return new Promise(resolve=>{const url=URL.createObjectURL(blob),img=new Image();img.onerror=()=>{URL.revokeObjectURL(url);resolve(blob)};img.onload=()=>{try{const c=document.createElement('canvas');c.width=img.naturalWidth;c.height=img.naturalHeight;const ctx=c.getContext('2d',{willReadFrequently:true});ctx.drawImage(img,0,0);const id=ctx.getImageData(0,0,c.width,c.height),d=id.data,sw=Math.min(192,c.width),sh=Math.max(1,Math.round(c.height*sw/c.width)),mini=document.createElement('canvas');mini.width=sw;mini.height=sh;const md=mini.getContext('2d',{willReadFrequently:true});md.drawImage(c,0,0,sw,sh);const m=md.getImageData(0,0,sw,sh).data,mask=new Uint8Array(sw*sh),seen=new Uint8Array(sw*sh);for(let i=0;i<mask.length;i++)mask[i]=m[i*4+3]>24?1:0;let best=[];const dirs=[1,-1,sw,-sw];for(let y=0;y<sh;y++)for(let x=0;x<sw;x++){const start=y*sw+x;if(!mask[start]||seen[start])continue;const stack=[start],comp=[];seen[start]=1;while(stack.length){const q=stack.pop();comp.push(q);for(const off of dirs){const nq=q+off;if(nq<0||nq>=mask.length)continue;const qx=q%sw,nx=nq%sw;if(Math.abs(nx-qx)>1||!mask[nq]||seen[nq])continue;seen[nq]=1;stack.push(nq)}}if(comp.length>best.length)best=comp}const keep=new Uint8Array(mask.length);best.forEach(q=>keep[q]=1);let minX=c.width,minY=c.height,maxX=-1,maxY=-1;for(let i=0;i<d.length;i+=4){const px=(i/4)%c.width,py=Math.floor(i/4/c.width),gx=Math.min(sw-1,Math.floor(px*sw/c.width)),gy=Math.min(sh-1,Math.floor(py*sh/c.height));let a=d[i+3];if(!keep[gy*sw+gx]||a<18)a=0;else if(a<70)a=Math.round(a*.52);d[i+3]=a;if(a>28){minX=Math.min(minX,px);minY=Math.min(minY,py);maxX=Math.max(maxX,px);maxY=Math.max(maxY,py)}}ctx.putImageData(id,0,0);if(maxX<0){URL.revokeObjectURL(url);return resolve(blob)}const px=Math.max(8,Math.round((maxX-minX)*.05)),py=Math.max(8,Math.round((maxY-minY)*.05)),sx=Math.max(0,minX-px),sy=Math.max(0,minY-py),ex=Math.min(c.width,maxX+px+1),ey=Math.min(c.height,maxY+py+1),out=document.createElement('canvas');out.width=ex-sx;out.height=ey-sy;out.getContext('2d').drawImage(c,sx,sy,out.width,out.height,0,0,out.width,out.height);out.toBlob(b=>{URL.revokeObjectURL(url);resolve(b||blob)},'image/webp',.88)}catch{URL.revokeObjectURL(url);resolve(blob)}};img.src=url})}
function detectColor(url){return new Promise(resolve=>{const img=new Image();img.onerror=()=>resolve({name:'',family:''});img.onload=()=>{try{const c=document.createElement('canvas');c.width=c.height=64;const x=c.getContext('2d',{willReadFrequently:true});x.drawImage(img,0,0,64,64);const d=x.getImageData(4,4,56,56).data,bins=new Map();for(let i=0;i<d.length;i+=4){const a=d[i+3]/255;if(a<.5)continue;const r=d[i],g=d[i+1],b=d[i+2],v=hsl(r,g,b);if(v.v>.975&&v.s<.08)continue;const qr=Math.round(r/20)*20,qg=Math.round(g/20)*20,qb=Math.round(b/20)*20,k=`${qr},${qg},${qb}`,w=a*(.7+Math.min(1,v.s*1.4)),z=bins.get(k)||{w:0,r:0,g:0,b:0};z.w+=w;z.r+=r*w;z.g+=g*w;z.b+=b*w;bins.set(k,z)}const top=[...bins.values()].sort((a,b)=>b.w-a.w)[0];if(!top)return resolve({name:'',family:''});const name=colorName(top.r/top.w,top.g/top.w,top.b/top.w);resolve({name,family:family(name)})}catch{resolve({name:'',family:''})}};img.src=url})}
const cat=s=>{s=String(s||'').toLowerCase();if(/dress/.test(s))return'dresses';if(/jeans|trouser|cargo|chino|shorts|skirt|pants/.test(s))return'bottoms';if(/sneaker|boot|loafer|sandal|heel|slide/.test(s))return'shoes';if(/jacket|coat|blazer|overshirt|hoodie|sweater|cardigan|suit/.test(s))return'outerwear';if(/bag|backpack|cap|hat|belt|watch|scarf|glasses/.test(s))return'accessories';return'tops'},sty=s=>{s=String(s||'').toLowerCase();if(/cargo|hoodie|jacket|overshirt|graphic|wide-leg/.test(s))return'streetwear';if(/blazer|loafer|oxford|suit|tailored/.test(s))return'smart';if(/sport|running|trainer|gym|athletic/.test(s))return'athletic';if(/utility|workwear/.test(s))return'utility';if(/vintage|retro|heritage|washed/.test(s))return'vintage';if(/polo|classic|varsity/.test(s))return'preppy';if(/minimal|plain|clean/.test(s))return'minimal';return'casual'},sil=s=>/oversized|boxy|baggy/.test(String(s||'').toLowerCase())?'oversized':/wide[- ]leg|wide|relaxed/.test(String(s||'').toLowerCase())?'relaxed':/slim|skinny|tapered|fitted/.test(String(s||'').toLowerCase())?'slim':'regular';
async function classify(blob,candidates=LABELS){const u=URL.createObjectURL(blob);try{return await classifier(u,candidates)||[]}finally{URL.revokeObjectURL(u)}}
async function analyse(file){await loadAI();const original=await toBlob(file),text=file.name.replace(/\.[^/.]+$/,'').replace(/[-_]+/g,' ').trim();status(true,'Scanning your piece…',32,P().lowPower?'Fast phone mode.':'High-quality mode.');const bgJob=(async()=>{try{return cleanCutout(await bg(original,{device:navigator.gpu?'gpu':'cpu',model:P().segmentationModel(),output:{format:'image/webp',quality:.88}}))}catch(e){console.warn('background removal failed',e);return original}})();let results=[];try{results=await classify(original)}catch(e){console.warn('classification failed',e)}let best=results[0]||{label:'',score:0};const cut=await bgJob;if(P().shouldRunSecondPass(best.score)){status(true,'Checking the garment…',64,'Refining only when the first read is uncertain.');try{const second=await classify(cut);if(second[0]&&(second[0].score>best.score||best.score<.25))best=second[0];if(second.length>results.length)results=second}catch{}}status(true,'Finishing details…',82,'Color comes from the isolated garment.');const data=await dataUrl(cut),col=await detectColor(data),label=best.label,category=best.score>.08?cat(label):cat(text),style=sty(`${label} ${text}`),silhouette=sil(`${label} ${text}`),seasonName=/linen|tank|shorts|sandal|summer|tee/.test(`${label} ${text}`.toLowerCase())?'summer':/wool|coat|puffer|fleece|thermal|winter|knit/.test(`${label} ${text}`.toLowerCase())?'winter':'all';status(false);return{id:uid(),name:title([col.name||'',style!=='casual'?style:'',title(label||(category==='tops'?'T-Shirt':category==='bottoms'?'Bottoms':category))].filter(Boolean).join(' ')),image:data,category,color:col.name||'neutral',colorFamily:col.family,style,season:seasonName,occasion:style==='smart'?'smart':style==='athletic'?'sport':'everyday',silhouette,pattern:/stripe/.test(`${text} ${label}`)?'stripe':/check|plaid/.test(`${text} ${label}`)?'check':/graphic|print/.test(`${text} ${label}`)?'graphic':'solid',warmth:category==='outerwear'?4:category==='shoes'?2:seasonName==='summer'?1:seasonName==='winter'?4:3,formality:style==='smart'?4:style==='preppy'?3:style==='athletic'?1:2,wearCount:0,favorite:false,aiIdentified:Boolean(label),visualConfidence:clamp(best.score),recognitionMargin:results[1]?clamp(best.score-(results[1].score||0)):clamp(best.score),aiAlternatives:results.slice(0,4).map(x=>({label:x.label,score:x.score||0})),metadataConfidence:clamp(best.score*.65+(col.name?.length?.25:.08)+.1),createdAt:Date.now()}}
function openResult(item){current=item;$('#previewImg').src=item.image;$('#fName').value=item.name;$('#fCategory').value=item.category;$('#fColor').value=item.color;$('#fStyle').value=item.style;$('#queueInfo').textContent=queue.length?`${queue.length} more piece${queue.length===1?'':'s'} to review`:`AI confidence ${Math.round((item.metadataConfidence||0)*100)}%`;$('#modal').hidden=false)}
function closeResult(){current=null;queue=[];$('#modal').hidden=true;status(false)}
function next(){if(queue.length)openResult(queue.shift());else closeResult()}
function save(){if(!current)return;current.name=String($('#fName').value||'').trim()||current.name;current.category=$('#fCategory').value;current.color=String($('#fColor').value||'').trim()||current.color;current.style=$('#fStyle').value;current.formality=current.style==='smart'?4:current.style==='preppy'?3:current.style==='athletic'?1:2;let items=[];try{items=JSON.parse(localStorage.getItem(STORE)||'[]')}catch{}if(!Array.isArray(items))items=[];items.unshift(current);try{localStorage.setItem(STORE,JSON.stringify(items))}catch{alert('Wardrobe storage is full. Remove an older piece first.');return}current=null;if(queue.length)next();else{status(false);$('#modal').hidden=true;window.location.reload()}}
async function startAIUpload(files){const list=[...(files||[])].filter(f=>f.type?.startsWith('image/'));if(!list.length)return;try{queue=[];for(const file of list)queue.push(await analyse(file));next()}catch(e){console.error(e);status(false);alert('Dolapy could not analyze that photo. Try one clear garment in frame.')}}
async function startCamera(){const modal=$('#cameraModal');if(!modal)return;modal.hidden=false;window.warmDolapyAI?.();if(!navigator.mediaDevices?.getUserMedia){$('#cameraNote').textContent='Camera access is unavailable here. Choose a photo from the device instead.';return}try{cameraStream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:896},height:{ideal:896},frameRate:{ideal:24,max:30}},audio:false});$('#cameraVideo').srcObject=cameraStream;$('#cameraNote').textContent='Center one piece in frame, then capture it.'}catch(e){console.warn(e);$('#cameraNote').textContent='Camera permission was blocked. Choose a photo from the device instead.'}}
function stopCamera(){cameraStream?.getTracks().forEach(t=>t.stop());cameraStream=null;if($('#cameraVideo'))$('#cameraVideo').srcObject=null}function closeCamera(){stopCamera();$('#cameraModal').hidden=true}
async function capturePhoto(){const v=$('#cameraVideo');if(!v?.videoWidth){alert('The camera is not ready yet.');return}const c=$('#cameraCanvas'),max=P().imageSize(),k=Math.min(1,max/Math.max(v.videoWidth,v.videoHeight));c.width=Math.max(1,Math.round(v.videoWidth*k));c.height=Math.max(1,Math.round(v.videoHeight*k));c.getContext('2d').drawImage(v,0,0,c.width,c.height);const b=await new Promise(r=>c.toBlob(r,'image/jpeg',.82));if(!b)return;closeCamera();await startAIUpload([new File([b],`dolapy-${Date.now()}.jpg`,{type:'image/jpeg'})])}
function bind(){document.addEventListener('click',e=>{const take=e.target.closest?.('#addHeroAI,#addWardrobeAI,#bottomAddAI,#emptyUpload,#wardrobeEmptyUpload');if(take){e.preventDefault();startCamera()}});$('#cameraGallery')?.addEventListener('click',()=>{closeCamera();$('#aiFileInput').click()});$('#closeCamera')?.addEventListener('click',closeCamera);$('#capturePhoto')?.addEventListener('click',capturePhoto);$('#closeModal')?.addEventListener('click',closeResult);$('#saveItem')?.addEventListener('click',save);$('#aiFileInput')?.addEventListener('change',e=>{const f=e.target.files;e.target.value='';startAIUpload(f)});$('#modal')?.addEventListener('click',e=>{if(e.target===e.currentTarget)closeResult()});$('#cameraModal')?.addEventListener('click',e=>{if(e.target===e.currentTarget)closeCamera()});document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(!$('#cameraModal')?.hidden)closeCamera();else if(!$('#modal')?.hidden)closeResult()}});window.addEventListener('beforeunload',stopCamera)}
window.startCamera=startCamera;window.startAIUpload=startAIUpload;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
